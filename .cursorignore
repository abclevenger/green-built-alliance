# Heavy trees — keeps Cursor indexing fast (see CX_PLAYBOOK / media plan)
public/mirror/**
public/wp-content/**
public/wp-admin/**
public/wp-includes/**

# Binaries — reference paths in TS only; do not load into AI context
public/**/*.jpg
public/**/*.jpeg
public/**/*.png
public/**/*.webp
public/**/*.gif
public/**/*.ico
